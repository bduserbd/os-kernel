# CPUs.
ifndef K_CONFIG_CPUS
  K_CONFIG_CPUS = 1
endif

BUILD_CPPFLAGS += -D K_CONFIG_CPUS=$(K_CONFIG_CPUS)

ifneq ($(K_CONFIG_CPUS),1)
  BUILD_CPPFLAGS += -D K_CONFIG_SMP
  export AP_BIN=ap.bin
endif

# Kernel stack size
BUILD_CPPFLAGS += -D K_CONFIG_STACK_SIZE=0x10000

